# Pinned to specific versions for reproducible builds.
# Run `pip install -r requirements.txt` to install.
# Run `pip freeze > requirements.txt` after adding new packages to update pins.
fastapi[all]==0.115.5
uvicorn[standard]==0.32.1
supabase>=2.31.0
pydantic>=2.11.7
pydantic-settings==2.6.1
python-dotenv==1.0.1
pytest>=7.4.0
httpx>=0.25.0
google-genai>=1.0.0
python-jose[cryptography]>=3.3.0
Pillow>=10.0.0
python-multipart>=0.0.9
